import React from 'react';
import ReactDOM from 'react-dom/client';
import { SpaceNavigatorApp } from './components/SpaceNavigatorApp';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SpaceNavigatorApp />
  </React.StrictMode>,
);
